apiVersion: v1
kind: Service

metadata:
  name: securebank
  namespace: devops-demo

spec:
  selector:
    app: securebank

  type: LoadBalancer

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
